/**
 * File contains a class named ClassRoom
 */

export default class ClassRoom {
  /**
   * This creates a class @see {@link ClassRoom}
   * @param {Number} maxStudentsSize - This shows the maximum
   * number of students.
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  } 
}
